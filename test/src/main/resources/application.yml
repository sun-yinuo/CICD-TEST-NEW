server:
  port: 8081

spring:
  application:
    name: test-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.81:8848
        namespace: public
        group: DEFAULT_GROUP

management:
  endpoints:
    web:
      exposure:
        include: '*'
